[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 600 --worker-class gevent --workers 1 --worker-connections 10"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
