[phases.setup]
nixPkgs = [
  "python311",
  "chromium",
  "chromedriver"
]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "mkdir -p /usr/bin",
  "ln -sf $(which chromium) /usr/bin/chromium || true",
  "ln -sf $(which chromedriver) /usr/bin/chromedriver || true"
]